/**
 * - keep: Specifies the item(s) that will not be spent on a recipe, but that's needed to keep in hand for crafting.
 * - destroy: Specifies the item(s) that will be destroyed after crafting.
 */
export type RecipeAction = 'keep' | 'destroy';
